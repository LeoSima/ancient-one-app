import { SenhasCoincidemDirective } from './senhas-coincidem.directive';

describe('SenhasCoincidemDirective', () => {
  it('should create an instance', () => {
    const directive = new SenhasCoincidemDirective();
    expect(directive).toBeTruthy();
  });
});
